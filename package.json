{
  "name": "dep-view",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "tsdown && nuxt build",
    "build:watch": "tsdown --watch",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "bin": {
    "dep-view": "./dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "chalk": "^5.6.2",
    "nuxt": "^4.2.2",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.7.3",
    "@types/node": "^25.0.3",
    "tsdown": "^0.18.3"
  }
}
